# GA4 Measurement ID from Admin → Data streams → your web stream (format G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=

# Optional: staging | production — shown in the build label (staging workflow sets this automatically)
VITE_DEPLOY_ENV=
